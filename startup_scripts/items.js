StartupEvents.registry("item", (event) => {
    // Chunk Ore
    event
        .create("graphite_chunk_mold")
        .displayName("Chunk Ore Mold")
        .maxStackSize(64);

    // iron chunk
    event
        .create("iron_chunk")
        .displayName("Iron Chunk")
        .maxStackSize(64);

   // copper chunk
    event
        .create("copper_chunk")
        .displayName("Copper Chunk")
        .maxStackSize(64);

// zinc chunk
    event
        .create("zinc_chunk")
        .displayName("zinc Chunk")
        .maxStackSize(64);

        // gold chunk
    event
        .create("gold_chunk")
        .displayName("Gold Chunk")
        .maxStackSize(64);

        });